# 产品需求文档（PRD）
## 校园二手图书自助回收售卖一体机

| 项目 | 内容 |
|---|---|
| 文档版本 | v0.2（已纳入关键决策） |
| 状态 | 草案 · 待开发 |
| 目标技术栈 | Python / FastAPI + MySQL + 本地/云 VLM |
| 现有代码基线 | [Schwarz52111/Used_book_recycle](https://github.com/Schwarz52111/Used_book_recycle) |
| 关联文档 | 项目计划书《第三章 项目产品与技术体系》 |
| 编写日期 | 2026-06-02 |

---

## 1. 背景与问题

高校师生每学期产生大量闲置教材与课外书，现有处置渠道（同学转手、二手群、废品回收、丢弃）存在三个痛点：**定价难**（卖家不知道值多少钱）、**交易匹配难**（买卖双方信息不对称、时空不匹配）、**信任难**（书况描述不可信、易纠纷）。结果是大量可循环图书被当废纸处理，既浪费又不环保。

本项目以校园内的自助一体机为载体，提供"投书—AI估价—回收—上架—自助购买"的闭环服务，用 AI 识别与数据化运营解决上述痛点，实现旧书循环利用与绿色低碳。

---

## 2. 目标与成功指标

### 2.1 产品目标

构建一套以 AI 识别和 agent 化定价为核心的二手图书回收与售卖系统，覆盖回收侧（用户投书、自动估价、入库）与售卖侧（浏览、购买、出货），并通过交易数据驱动动态定价与回收准入。

### 2.2 成功指标（首个校园试点学期）

- 回收侧：单台设备月均回收 ≥ 300 册；AI 一次识别成功率 ≥ 85%（命中书目+ISBN）；品相评估与人工复核一致率 ≥ 80%。
- 售卖侧：入库书目售出周转率 ≥ 40%/学期；交易纠纷率 ≤ 2%。
- 运营：估价毛利率为正；人工复核工作量 ≤ 总回收量的 20%（其余自动通过）。

> 以上为占位目标，需结合试点设备数量与校方数据在评审时校准。

---

## 3. 用户与核心场景

**主要用户**：校园师生（卖家=投书者，买家=购书者）。
**次要用户**：运营/复核人员（处理低置信度估价、库存调度）。

核心场景：

1. **回收（卖书）**：用户把书放到设备识别区 → 系统识别书目与品相 → 给出回收价 → 用户确认 → 投入设备 → 到账（校园卡/微信）。
2. **购买（买书）**：用户在设备屏幕或小程序浏览在库书 → 选书 → 支付 → 设备出货/自取。
3. **复核**：低置信度的识别或估价进入人工复核队列，运营人员在后台确认或修正后再生效。
4. **运营**：系统根据热度与库存，动态调整回收率、准入名单与售价（滞销降价/捐赠建议）。

---

## 4. 现状评估（基于现有代码基线）

现有仓库是一个**可运行的单机识别 demo**，不是系统。本节明确"复用什么、替换什么、新增什么"。

**可复用**

- 识别链路思路：ISBN 条码（pyzbar）→ OCR（Tesseract chi_sim）→ 本地 VLM（Ollama `qwen2.5vl:3b`）兜底 → 与 `books` 表模糊匹配。链路设计合理，迁移为后端服务后保留。
- 数据库雏形：`books` / `condition_rules` / `recycle_records` 三表，查询参数化（无注入风险）。作为初始模型保留并扩展。

**必须替换**

- **品相评估（最高优先级）**：现有 `estimate_condition()` 用 Canny 边缘均值当"破损分"、边框像素方差当"完整度"，与真实书况（水渍、划线、缺页、破损封面）无因果关系，不可用。改为 **VLM 结构化评分 + 人工复核**。
- **定价**：现为一行公式 `market_price × base_recycle_rate × price_factor`，升级为规则+市场比价+热度+库存的定价引擎/agent。
- **书目数据源**：现依赖手工种子 `books` 表，真实书目几乎匹配不上。接入真实 ISBN 元数据来源。

**必须新增**

- 后端服务与 API（当前只有脚本 + OpenCV 窗口）。
- 售卖侧全流程（库存、商品、UI、支付、出货）。
- 用户账户与权益、复核后台、热度分析、多机运营。

**工程问题**

- DB 密码 `123456` 硬编码在示例与文档中 → 改为环境变量/密钥管理。
- 无测试、无配置分层、无错误上报。

---

## 5. 功能需求

> 标注：P0=首期必须，P1=次期，P2=远期。每条尽量映射到现有代码或新增模块。

### 5.1 图书识别（P0）— `识别 agent`

- F-1（P0）多模态识别：依次尝试 ISBN 条码 → OCR → VLM；任一命中即返回。复用现有链路，封装为 `/recognize` 服务接口。
- F-2（P0）真实书目元数据：当本地 `books` 表未命中时，调用外部 ISBN 元数据源（如开放 ISBN/图书 API）补全书名、作者、出版社、定价、分类，并落库。
- F-3（P1）agent 化自纠：识别置信度低时，可请求用户更换拍摄角度或补拍书脊/版权页，多轮收敛，而非一次失败即放弃。
- F-4（P0）识别结果含置信度，低于阈值进入复核队列（见 5.5）。

### 5.2 品相评估（P0）— `品相 agent`（替换假CV）

- F-5（P0）结构化评分：VLM 按统一 rubric 输出各维度评分与证据——封面磨损、书脊、污渍/水渍、划线/笔记、折角、缺页/缺光盘，给出综合品相等级（like_new/good/acceptable/damaged）+ 置信度 + 文字说明。
- F-6（P0）证据留存：保存识别照片与评分依据，供复核与纠纷追溯。
- F-7（P1）拒收判定：严重破损/盗版/禁书/无价值书目自动拒收并给出原因。

### 5.3 定价（P0）— `定价 agent`

- F-8（P0）回收定价：综合 `品相评分 × 市场参考价 × 基础回收率 × 热度系数 × 库存系数`，输出回收价与定价理由（可解释）。
- F-9（P1）市场比价：agent 拉取二手市场公开比价（如多抓鱼/孔夫子等）作为参考锚点，提升定价可信度。
- F-10（P0）售价定价：基于回收成本、品相、热度生成上架售价，保证毛利为正。
- F-11（P1）动态调价：滞销书自动降价、超储书降低回收率，热门书提高回收率。

### 5.4 库存管理（P0/P1）

- F-12（P0）入库：回收确认后生成库存条目（书目、品相、成本价、售价、库位、状态）。
- F-13（P0）状态机：库存状态 在库 → 已售/退回/捐赠/报废，全程可追溯。
- F-14（P1）库容与库位管理：单机容量约束、满仓预警。
- F-15（P2）多机调拨：跨设备库存再分配建议。

### 5.5 人工复核（P0）— 复核后台

- F-16（P0）复核队列：低置信度识别/估价、拒收申诉、异常交易进入队列。
- F-17（P0）复核操作：运营人员可确认/修正书目、品相、价格，修正结果回流用于模型与规则优化。

### 5.6 售卖与交易（P0）

- F-18（P0）浏览：设备屏幕/小程序按分类、关键词、价格浏览在库书。
- F-19（P0）下单支付：微信/支付宝/校园卡支付，订单与支付状态管理。
- F-20（P0）出货/自取：与设备出货机制对接（或自取码取书）。
- F-21（P1）推荐：基于课程/历史的购书推荐（顾客助手 agent）。

### 5.7 用户账户与权益（P0/P1）

- F-22（P0）身份：微信/校园卡登录，绑定收款账户。
- F-23（P0）回收到账：回收金额到账，交易明细可查。
- F-24（P1）权益机制：积分、信用分（恶意投书/虚假书况降分）、差异化回收率。

### 5.8 热度分析与准入调控（P1/P2）— `热度 agent`

- F-25（P1）热度计算：基于交易、浏览、课程目录、学期时点计算书目热度。
- F-26（P1）动态准入：超储/冷门书目降低或暂停回收，热门书目优先回收。
- F-27（P2）需求预测：按学期周期预测教材需求，指导回收与备货。

### 5.9 运营与数据（P1）

- F-28（P1）运营看板：回收量、周转率、毛利、复核工作量、设备状态。
- F-29（P1）滞销处理：滞销自动降价/捐赠/报废建议。

---

## 6. 非功能需求

- **性能**：单次识别端到端 ≤ 8s（本地 VLM 视硬件而定，提供云 VLM 选项）；接口 P95 ≤ 1s（识别接口除外）。
- **可用性**：设备离线时回收/购买核心流程可降级（本地缓存书目+延迟对账）。
- **安全合规**：支付与个人信息合规；密钥不入代码库；识别照片最小化留存并脱敏。
- **可维护性**：服务分层、配置外置、关键路径有测试与日志。
- **可观测性**：识别/估价/交易全链路埋点，便于复核与调优。

---

## 7. 系统架构（目标态）

```
                 ┌────────────────────────────────────────┐
   设备 Kiosk ──▶│  Kiosk 前端（Web/Electron）             │
   微信小程序 ──▶│  + 小程序前端                            │
                 └───────────────┬────────────────────────┘
                                 │ REST
                 ┌───────────────▼────────────────────────┐
                 │            FastAPI 后端                   │
                 │  /recognize  /price  /inventory          │
                 │  /order  /pay  /review  /analytics       │
                 └───┬─────────┬─────────┬─────────┬───────┘
                     │         │         │         │
              ┌──────▼──┐ ┌────▼─────┐ ┌─▼──────┐ ┌▼────────┐
              │识别服务  │ │定价 agent│ │库存/订单│ │热度分析  │
              │条码/OCR/ │ │规则+比价 │ │ 状态机  │ │ + 准入   │
              │ VLM     │ │+品相+库存│ │         │ │ 调控     │
              └────┬────┘ └────┬─────┘ └────┬───┘ └────┬────┘
                   │           │            │          │
          ┌────────▼───────────▼────────────▼──────────▼───────┐
          │  MySQL（书目/库存/订单/复核/交易）  +  对象存储(照片) │
          │  外部：ISBN元数据API、二手市场比价、支付网关          │
          └─────────────────────────────────────────────────────┘
```

**agent 编排**：定价、识别、热度三个 agent 用轻量编排（LangGraph 或自定义工具循环）实现，统一定义工具（DB 查询、元数据查询、比价抓取、库存查询、人工复核挂起）。VLM **采用云部署为主**（成本与维护更可控），保留本地 Ollama `qwen2.5vl` 作为离线/降级选项。

### 7.1 硬件载体与约束（复用既有智能书柜）

前期复用市场上现有的智能书柜（玻璃门、书籍**书脊朝外竖插于固定货道**、中部触摸屏、下方取书区），不做硬件改造。由此带来的约束：

- **识别在中央工位完成**：用户投书时在中部摄像/识别工位完成书目+品相识别，而非每个货道单独识别。
- **库存按货道管理**：每本书占一个货道，单机容量有限，需库位/满仓管理。
- **出货/取书机制待定（见 §13）**：现有书柜通常为两类——(a) 机械货道售卖式出货，或 (b) RFID 电子门自助开门取书 + 自动核验。两者对"是否每本书需贴 RFID 标签""购买时如何确认取了哪本"影响很大，是当前唯一未定的硬件细节。软件层先做成**可适配两种机制的抽象出货接口**，待确定具体型号再落地。

---

## 8. 数据模型（在现有三表上扩展）

- `books`（保留+扩展）：增加封面图、来源、热度字段。
- `condition_rules`（保留）：品相等级与系数。
- `recycle_records`（保留+扩展）：关联库存与复核状态、识别照片、agent 定价理由。
- 新增 `inventory`（库存：书目、品相、成本价、售价、库位、状态）。
- 新增 `orders` / `payments`（订单与支付）。
- 新增 `users`（账户、收款、信用分）。
- 新增 `review_tasks`（复核队列）。
- 新增 `book_metadata_cache`（外部 ISBN 元数据缓存）。
- 新增 `demand_signals`（热度/需求信号）。

---

## 9. 技术栈

- **后端**：Python 3.11 + FastAPI + SQLAlchemy；MySQL（或 PostgreSQL）；对象存储（MinIO/OSS）。
- **识别**：复用 pyzbar / Tesseract（chi_sim）/ OpenCV；VLM **以云 VLM 为主**，本地 Ollama `qwen2.5vl:3b` 作离线降级。
- **agent**：LangGraph 或自定义工具循环；工具=DB/元数据/比价/库存/复核。
- **前端**：Kiosk Web（React）；**微信小程序作为购书与查账主入口**（设备屏用于投书回收与就地购买）。
- **支付**：**微信支付优先**，预留支付宝/校园卡扩展。
- **运维**：环境变量+密钥管理、Docker 化、基础 CI 与测试。

---

## 10. 分阶段路线与里程碑

**Phase 0 — 夯实 MVP（核心可信度）**
- 用 VLM `品相 agent` 替换假 CV 评分；接入真实 ISBN 元数据；密钥/配置治理；错误处理与日志。
- 里程碑：识别+品相+估价以服务形式跑通，估价可解释。

**Phase 1 — 变成产品**
- 脚本重构为 FastAPI 后端（`/recognize`、`/price`、`/inventory`、`/order`、`/pay`）；定价引擎；**售卖侧全流程**（库存、商品、Kiosk/Web UI、支付、出货）；账户与到账；复核后台。
- 里程碑：单台设备完成"投书→估价→入库→上架→购买→出货"完整闭环。

**Phase 2 — 数据 + agent**
- 交易数据仓库；热度分析；agent 化定价与准入；复核结果回流优化。
- 里程碑：动态回收率与准入名单上线，毛利可见。

**Phase 3 — 规模化**
- 多机运营、微信小程序商城、推荐、捐赠/再分配。

---

## 11. 验收标准（Phase 0/1）

- `/recognize` 对真实书籍（含 ISBN 与无 ISBN 封面）识别成功率达标，返回书目+置信度。
- 品相评估输出结构化评分+证据+置信度，低置信度可正确路由到复核。
- 估价输出回收价、售价及可读的定价理由，毛利为正。
- 售卖闭环：可在 UI 浏览在库书、下单、支付、生成出货/自取凭证。
- 安全：无硬编码密钥，照片留存最小化。
- 关键路径有自动化测试。

---

## 12. 风险与应对

| 风险 | 说明 | 应对 |
|---|---|---|
| "纯软件零硬件"叙事不成立 | 回收/出货需物理仓与机构 | 明确定位：复用现有一体机物理仓；项目书统一口径 |
| AI 品相误判 | 误判导致亏损或纠纷 | 结构化 rubric + 置信度阈值 + 人工复核 + 证据留存 |
| 书目元数据缺失 | 真实书匹配不上 | 接入外部 ISBN 元数据并缓存落库 |
| 恶意投书/虚假书况 | 用户塞错书/破书套现 | 投书后二次核验 + 信用分 + 复核拦截 |
| 估价亏损 | 定价偏高滞销 | 库存/热度系数 + 动态调价 + 滞销处置 |
| 政策合规 | 支付、隐私、校园准入 | 合规支付通道、数据脱敏、校方合作授权 |

---

## 13. 关键决策（已确认）

1. **硬件**：纯软件，复用市场上现有智能书柜，不做硬件改造（见 §7.1）。
2. **VLM**：允许云部署，以云 VLM 为主，本地为降级。
3. **支付**：微信支付优先，预留支付宝/校园卡。
4. **节奏**：软件层先完善成完整产品，再做校园试点。
5. **小程序**：需要微信小程序，作为购书与查账主入口。

### 13.1 仍待确认（不阻塞软件开发）

- **出货/取书机制**：目标书柜是机械货道售卖式，还是 RFID 电子门自助式？决定是否需要 RFID 贴标与取书核验方式。软件先用抽象出货接口适配两种，待具体型号确定再对接。
- 首个试点设备数量与可获得的校方数据范围（影响热度分析冷启动）。
