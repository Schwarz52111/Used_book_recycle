# 项目进展同步 · Phase 1（订单/支付/账户/小程序）

**项目**：校园二手图书自助回收售卖一体机（即刻小绿）
**仓库**：https://github.com/Schwarz52111/Used_book_recycle （PR 来自 koljiu753 fork）
**日期**：2026-06-03
**接续**：在 Phase 0（识别/品相/定价/库存/触屏界面）基础上推进

---

## 一、本轮新增

**1. 订单系统**
完整状态机：下单锁库存 → 支付 → 自动出货 → 完成；支持取消并释放库存，防止并发重复售卖。

**2. 支付抽象层（模拟 + 微信支付）**
开发期用「模拟支付」即可跑通全流程；真实「微信支付」做成预留适配器，拿到商户凭证后填上即可，业务层不动。

**3. 用户账户与账本**
微信 openid 登录的用户体系，含余额与信用分；每笔余额变动走账本流水（回收到账/购买/退款），保证"余额=流水之和"可对账。

**4. 回收到账打通**
确认回收时，回收金额自动计入卖家账户余额并记流水。

**5. 微信登录（模拟 + 真实）**
小程序 `wx.login` 的 code 换 openid；开发期 mock 模式无需 AppID，真实模式接 `jscode2session`。

**6. 微信小程序购书入口**（新增 `miniprogram/`）
三个页面：买书（在库书网格）、书籍详情（下单+支付+出货）、我的（登录/余额/信用分）。可在微信开发者工具直接运行。

**7. 工程**
后端开启 CORS；初始化脚本补充示例在库书；修复 SQLite 下 BigInteger 主键不自增的问题；新增订单/账户单元测试，全部通过（共 10 项）。

---

## 二、怎么跑

**后端**（`backend/`）：
```
python -m scripts.init_db        # 建表 + 示例书 + 示例在库书
uvicorn app.main:app --reload
```

**小程序**（`miniprogram/`）：
微信开发者工具 → 导入 `miniprogram` 文件夹 → AppID 选「测试号」→ 详情里勾「不校验合法域名」→ 编译。详见 `miniprogram/README.md`。

---

## 三、需要团队配合 / 申请的事

1. **微信小程序 AppID**：去微信公众平台注册小程序，拿到 AppID/Secret（真实登录、上线都需要）。谁来牵头？
2. **微信支付商户号**：申请微信支付（商户号、APIv3 密钥、证书、已备案 HTTPS 域名），用于接入真实支付。
3. **出货机制**（仍待定）：目标书柜是机械货道还是 RFID 电子门，决定要不要贴 RFID。
4. **书目元数据源**：稳定的 ISBN 元数据接口（书名/作者/定价）。

---

## 四、下一步可选方向

接入真实微信支付 / 人工复核后台 / Phase 2 热度分析与动态回收准入。
