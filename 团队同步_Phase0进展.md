# 项目进展同步 · Phase 0 完成

**项目**：校园二手图书自助回收售卖一体机（即刻小绿）
**仓库**：https://github.com/Schwarz52111/Used_book_recycle
**日期**：2026-06-03
**本轮负责人**：（请填）

---

## 一、本轮完成了什么

把原来那个"单机摄像头识别 demo"升级成了一套**可运行的后端服务 + 设备触屏界面**，并产出了完整的产品需求文档（PRD）。具体四块：

1. **识别服务**（`/recognize`、`/appraise`）
   复用并重构了原型的「ISBN 条码 → OCR → VLM」识别链路，封装成 HTTP 接口；本地书目匹配不到时，会用 ISBN 调外部元数据接口补全并落库，解决了"真实书几乎匹配不上"的老问题。

2. **AI 品相评估 agent**（重点）
   彻底替换了原型里那段**假的品相评分**（原来是拿图像边缘均值冒充"破损分"，和真实书况没关系）。现在用视觉大模型（接的是 Moonshot/Kimi 的 `moonshot-v1-8k-vision-preview`）按 6 个维度（封面磨损、书脊、污渍、划线、折角、缺页）结构化打分，给出证据、综合等级和置信度；置信度低的自动转人工复核。

3. **可解释定价引擎**
   回收价 = 市场价 × 回收率 × 品相系数 × 热度系数 × 库存系数，售价按品相比例与加价取高。每次定价的系数和算式都写进结果里，**可解释、可复核**，不再是一行黑箱公式。

4. **库存 + 出货抽象接口**
   新增库存模型与状态机（在库/已售/退回/捐赠/报废）；出货层做成抽象接口，可适配"机械货道"和"RFID 电子门"两类书柜，等硬件型号定了只换适配器。

另外：
- **设备触屏界面**：首页双入口（卖书/买书），卖书走"拍照→识别品相估价→确认回收"，买书走"在库网格→下单→出货"，用 Forest Canopy 主题做了正式的产品级视觉。
- **工程化**：密钥全部走 `.env`（去掉了原型里硬编码的数据库密码）；加了 `.gitignore` 保护密钥与本地库；单元测试已通过；本地服务可正常启动访问。
- **PRD 文档**：`PRD_校园二手图书自助回收售卖一体机.md`，含现状评估、功能需求、架构、数据模型、分阶段路线、风险。

---

## 二、技术栈

Python + FastAPI + SQLAlchemy；数据库 MySQL（本地可用 SQLite 快速试跑）；视觉识别 Moonshot/Kimi 云 VLM（可降级本地 Ollama）；前端为单页触屏界面（原生 HTML/JS，FastAPI 同源托管）。

---

## 三、怎么跑起来（给技术同学）

```
cd backend
python -m venv .venv && .venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env        # 填数据库与 VLM key；想零配置就用 .env 里的 SQLite 开关
python -m scripts.init_db
uvicorn app.main:app --reload
```
浏览器开 http://127.0.0.1:8000/ 看界面，/docs 看接口。

---

## 四、下一步（Phase 1）

- 微信支付 + 订单系统（目前支付是占位）
- 用户账户与回收到账
- 微信小程序购书入口
- 热度分析与动态回收准入
- 人工复核后台界面

---

## 五、需要团队拍板/配合的事

1. **出货机制**：目标书柜是「机械货道售卖式」还是「RFID 电子门自助式」？决定要不要给每本书贴 RFID。（不阻塞当前开发，但影响 Phase 1）
2. **书目数据来源**：需要一个稳定的 ISBN 元数据来源（书名/作者/定价），有没有同学能对接或采购？
3. **试点设备**：首批能拿到几台、放在哪个校区？影响热度分析的冷启动。
4. **第三章文档**：技术体系初稿需要根据本轮实际实现回头校对一遍（识别/品相/定价的描述要和代码一致）。
